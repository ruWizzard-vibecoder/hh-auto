import { Shell } from '@/components/layout/shell';
import { SummariesView } from '@/features/summaries/summaries-view';

export default function SummariesPage() {
  return (
    <Shell>
      <SummariesView />
    </Shell>
  );
}
